import React from 'react';
import styled from 'styled-components';

const ScroolWrapStyle = styled.div``;

function ScrollWrap(props) {
  return <ScroolWrapStyle {...props} />;
}

export default ScrollWrap;
